# Production Environment Variables for Docker Compose
# Copy this file to .env.production and update with your values

# REQUIRED: Strong random secret for JWT tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# OPTIONAL: Logging level
LOG_LEVEL=info

# OPTIONAL: CORS origins (comma-separated URLs)
# CORS_ORIGINS=https://orchestrator.yourdomain.com,https://backup.yourdomain.com

# OPTIONAL: Custom database path (inside container)
# DB_PATH=/root/data/orchestrator.db
